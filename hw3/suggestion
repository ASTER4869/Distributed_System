作业1需要先将大文件进行分块，按理来说是可以将这一整个文件载入内存处理的，但实际运行起来非常慢。大文件中一些信息是对作业来说没有用的，可以先预处理，留下姓名、年份等关键信息。
改进后的思路是端口启动后，立马解析分块文件，形成姓名、年份的二维哈希表，这样就可以加快速度。但是，
很逆天的一点是，数据里面的姓名不规范，你不知道到底该把某些名字算不算进来，所以最后查询的结果也没有可以参考的正确答案。
作业2是在手动启动后能完成基本要求后，编写脚本同时启动多个服务器端口进行演示，并可视化展示。我没有这么做，但是因为当时是线上，所以也好运的过了。
